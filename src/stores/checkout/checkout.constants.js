export  const ActionTypes = {
    CREATE_ADRESS : 'CREATE_ADRESS', 
    ADD_SHIPPING_METHOD : 'ADD_SHIPPING_METHOD'
    /* if guest <createEmptyCart>  */
    /* if customer <customerCart>  */
    /* if guest and then customer connct we must use <mergeCarts>   */
}